---
layout: default
---
<h2>Upcoming Events</h2>
<ul>
    {% for event in site.events %}
        {% if date(event.date) >= date('today') %}
            <li>
                {{ include('event-teaser') }}
            </li>
        {% endif %}
    {% endfor %}
</ul>

<h2>Previous Events</h2>
<ul>
    {% for event in site.events|reverse %}
        {% if date(event.date) < date('today') %}
            <li>
                {{ include('event-teaser') }}
            </li>
        {% endif %}
    {% endfor %}
</ul>
